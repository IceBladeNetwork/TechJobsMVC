<h1>@ViewBag.title</h1>
    <h3>@ViewBag.jobs.Count Result(s)</h3>

    @for (int i = 0; i < ViewBag.Jobs.Count; i++)
    {
        @foreach (KeyValuePair<string, string> column in ViewBag.jobs[i])
        {
            <table>
                <tr>
                    <td>@column.Key</td>
                    <td>@column.Value</td>
                </tr>
            </table>
        }
    }
    